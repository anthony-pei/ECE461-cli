 #!/usr/bin/env bash

subdir="cli"

if [[ "$1" == "build" ]]; then
  echo "build"
elif [[ "$1" == "install" ]]; then
  echo "install dependencies"
elif [[ "$1" == "test" ]]; then
  echo "running tests"
elif [[ -n "$1" ]]; then
  echo "checking URL"
  cd "$subdir"
  go run main.go input.txt
fi
